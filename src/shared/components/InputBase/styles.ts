import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  input: {
    padding: 10,
    flex: 1,
  },
  rightItem: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
export default styles;
